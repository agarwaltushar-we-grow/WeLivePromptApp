# Error Handling

Handled cases include empty input, unsupported language, missing API key, API provider failure, Ollama connection failure, malformed LLM JSON, translation/model failures, and unexpected exceptions. Secrets are never stored in source code. Exported prompts may contain user text.
